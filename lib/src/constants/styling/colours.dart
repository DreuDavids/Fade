import 'package:flutter/material.dart';

const primaryColor = Color(0XFF1E2630);
const darkGrey = Color(0XFF424242);
const darkBlue = Color(0XFF353F4B);
const yellow = Color(0xFFE3B347);
const dividerColour = Color(0XFF688698);
const darkBackground = Color(0XFF0A0C0F);

//used for fonts
const white = Color(0xFFFFFFFF);
final feintWhite = Colors.white.withOpacity(0.6);

//used for the title underline decorations
final feintWhiteUnderline = Colors.white.withOpacity(0.2);

//used for the blurs
final socialIconsBlur = const Color(0XFF292828).withOpacity(0.4);
final blurOverlayColor = primaryColor.withOpacity(0.5);
final titleBlurOverlayColor = const Color(0XFF292828).withOpacity(0.4);
